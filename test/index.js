'use strict';

var should = require('chai').should(),
    normalizr = require('../index');

describe('normalizr', function () {
  it('returns TODO', function (){
    normalizr.should.equal('TODO');
  });
});
